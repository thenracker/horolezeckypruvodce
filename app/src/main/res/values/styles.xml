<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <item name="android:popupMenuStyle">@style/AppTheme.PopupOverlay</item>
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">

        <!--<item name="android:background">#FFFFFF</item>
        <item name="android:backgroundTint">#DDDDDD</item> zvěrstva-->

    </style>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" >
        <!--<item name="android:colorBackground">@color/DefaultBackgroundColor</item>-->
        <item name="android:colorBackground">@color/DefaultBackgroundColor</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <!--<item name="android:titleTextColor">@color/colorAccent</item>-->
    </style>


    <!-- TAB LAYOUT -->
    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">

        <item name="tabBackground">@color/colorTabLayout</item>
        <item name="tabTextColor">@color/colorTabLayoutFont</item>
        <item name="tabSelectedTextColor">@color/colorAccent</item>
        <item name="tabTextAppearance">@style/MyCustomTextAppearance</item>
        <item name="android:textSize">20sp</item>

        <item name="tabPaddingStart">0dp</item>

    </style>
    <style name="MyCustomTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
    </style>

     <!-- SPLASH SCREEN -->
</resources>
